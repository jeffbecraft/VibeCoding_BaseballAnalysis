# Streamlit Cloud Requirements
# Minimal dependencies for cloud deployment

# Core dependencies
streamlit>=1.51.0
pandas>=2.0.0
numpy>=1.24.0
requests>=2.31.0
python-dotenv>=1.0.0
tenacity>=8.2.0

# Visualization
plotly>=5.14.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Data analysis
scipy>=1.10.0
statsmodels>=0.14.0

# Optional: Error monitoring (FREE tier available)
# Uncomment if you want Sentry monitoring:
# sentry-sdk>=1.40.0

# Optional: OpenAI for AI queries (COSTS MONEY - not included by default)
# Only uncomment if you plan to use OpenAI and have an API key:
# openai>=1.10.0

# Note: Ollama is NOT available on Streamlit Cloud (local only)
# The app will work fine without AI - it falls back to standard queries
