# Streamlit Cloud Requirements
# Minimal dependencies for cloud deployment

# Core dependencies
streamlit>=1.51.0
pandas>=2.0.0
numpy>=1.24.0
requests>=2.31.0
python-dotenv>=1.0.0
tenacity>=8.2.0

# Visualization
plotly>=5.14.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Data analysis
scipy>=1.10.0
statsmodels>=0.14.0

# Optional: Error monitoring (FREE tier available)
# Uncomment if you want Sentry monitoring:
# sentry-sdk>=1.40.0

# OpenAI for AI queries (requires API key and costs money per query)
# Enables advanced natural language queries beyond standard patterns
openai>=1.10.0

# Note: Ollama is NOT available on Streamlit Cloud (local only)
# With OpenAI enabled, the app supports both standard queries AND AI-powered queries
